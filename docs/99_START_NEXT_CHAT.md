# HISTORICAL — 현재 기준 아님

현재 작업 진입점은 `AGENTS.md`다. 아래 내용은 과거 시작 지시이며 현재 기준으로 실행하지 않는다.

# Start Next Chat

Use Git `main` at annotated tag `v0.3.0` as the official source baseline. Verify `BASELINE.json`, version surfaces, clean working tree, Integration health, disabled protected controls, and the published SHA of `GUI_Arc_Integrated_v0.3.0_Integrated.zip` before editing.

Create a feature or hotfix branch. Never develop directly on `main`; never commit release ZIPs, secrets, credentials, sessions, test PINs, local D1/R2 data, or sensitive screenshots. Production and legacy Platform, OS and Issue resources require separate explicit approval.

Read Constitution, Architecture, Git Release Workflow, Today Module Contract, Today Provider Contract, Today Browser E2E, Issue contracts and Next Chat Handoff before deciding scope. Preserve one Integrated session and canonical Core/Issue ownership.
# v0.4.0 next scope

Start from Git `main` tag `v0.4.0` and verify the published Integrated ZIP hash. Configure the Integration-only QR signing secret through Worker secret management, provision approved role-scoped fixtures without committing credentials, then execute authenticated Admin/FIELD_WORKER/camera/Today-count E2E. Do not change Production. Daily report, TBM, education documents, check-out, adjustments and device-management workflows remain NOT_IMPLEMENTED.
