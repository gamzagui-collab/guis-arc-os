# Authenticated Browser E2E

Use an approved real account only. Mock accounts, cookie injection, direct D1 user creation, and permission elevation are forbidden.

- Login and one Integrated session
- Desktop and Mobile More accordion: current module open on direct URL/reload, one group open, Enter/Space, ARIA state
- Canonical document route: 보관함 `/documents/archive`; `/documents/reports` is rejected as non-canonical
- Site context authorization and audit
- Logout revocation and protected-route 401
- Session expiry and contextVersion invalidation
- 360/390/412px mobile overflow and 44px targets

Authenticated Browser E2E is not PASS until actual credentials are supplied and evidence is recorded.

