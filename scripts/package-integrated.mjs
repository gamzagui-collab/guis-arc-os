import fs from"node:fs";import crypto from"node:crypto";import{spawnSync}from"node:child_process";const version=JSON.parse(fs.readFileSync("package.json","utf8")).version,name=`GUI_Arc_Integrated_v${version}_Integrated.zip`;fs.mkdirSync("releases",{recursive:true});const output=`releases/${name}`,include=["AGENTS.md","DEVELOPMENT_RULES.md","VERSION.md","AIOS",".agents","apps","database","docs","packages","scripts","tests","worker",".gitignore","BASELINE.json","build-metadata.json","CHANGELOG.md","package.json","package-lock.json","README.md","r2-cors.integration.json","wrangler.integration.toml","wrangler.pages.toml"];fs.rmSync(output,{force:true});const result=spawnSync("tar",["-a","-c","-f",output,...include],{stdio:"inherit"});if(result.status)process.exit(result.status);const hash=crypto.createHash("sha256").update(fs.readFileSync(output)).digest("hex");fs.writeFileSync(output+".sha256",`${hash}  ${name}\n`);console.log(`${output}\nSHA-256 ${hash}`);
