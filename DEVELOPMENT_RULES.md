# GUI's Arc 핵심 개발규칙

## ChatGPT와 Codex의 개발 역할

GUI's Arc의 기본 개발 운영은 ChatGPT와 Codex의 역할을 분리한다.

### ChatGPT 기본 역할

ChatGPT는 개발 설계·기술 관리 책임자다. 사용자 요구사항 정리, 예상 문제·부작용·충돌·운영 위험 분석, 아키텍처·DB·권한·UX 설계, 구현 범위와 우선순위 결정, Codex에 전달할 공식 개발 프롬프트 작성, Codex 구현 결과 검토, 미실행 항목 구분과 다음 개발 방향 결정을 담당한다. 기본적으로 공식 프로젝트의 코드·Migration·배포·ZIP을 직접 변경하지 않는다.

### Codex 기본 역할

Codex는 공식 프로젝트 구현 책임자다. 공식 프로젝트 루트의 코드 수정, Migration 작성, 자동 테스트와 Browser E2E, Typecheck·Validate·Build, 승인된 Integration 및 Production 배포, Git 상태·Commit·Tag 관리, Integrated ZIP과 SHA-256 생성, 공식 최신 소스와 결과물 유지를 담당한다.

### 공식 개발 흐름

1. 사용자가 현장 요구를 설명한다.
2. ChatGPT가 요구·위험·구조를 분석하고 Codex용 공식 프롬프트를 작성한다.
3. Codex가 공식 프로젝트에서 구현·검증·패키징한다.
4. 사용자가 Codex 결과를 ChatGPT에 전달한다.
5. ChatGPT가 구조·위험·누락·결과를 검토한 뒤 다음 개발 또는 배포를 결정한다.

Codex 결과 검토 없이 과거 계획이나 다음 기능을 자동으로 시작하지 않는다.

### ChatGPT 직접 구현 예외

Codex 장애·사용 불가·긴급 수정 또는 사용자가 명시적으로 ChatGPT 직접 구현을 선택한 경우에만 ChatGPT가 코드 수정이나 ZIP 생성을 수행할 수 있다. 이 경우 구현 주체, Codex 공식 루트 자동 갱신 여부, 결과물의 공식·임시 여부와 추후 동기화 필요성을 시작 전에 알려야 한다. 사용자 승인 없이 ChatGPT가 Codex의 공식 구현 역할을 대체하거나 ChatGPT 결과물을 공식 프로젝트에 자동 반영된 것으로 간주하지 않는다.

## 사용자와 작업 범위

- 사용자는 현장 업무 요구와 불편만 설명하면 된다. 구현·검증·버전 판단은 Codex가 담당한다.
- 기본 방식은 `최소 범위 수정`이다. 관련된 파일만 읽고 저장소 전체 분석·감사·리팩터링을 기본적으로 하지 않는다.
- 수동 파일 병합, Git 선택, 복잡한 배포 판단을 사용자에게 맡기지 않는다.
- 기존 메뉴, 권한, 인증, 데이터, DB, R2, Worker와 다른 모듈 원본을 요청 없이 변경하지 않는다.

## 화면과 사실성

- 사용자 화면은 한국어를 기본으로 하고 현장 핵심 정보를 우선한다.
- Mock, UI-only, Local-only, 미연결 상태를 실제 구현이나 배포 완료로 보고하지 않는다.
- 수행하지 않은 검증은 `NOT_EXECUTED`, 실패는 `FAIL`, 통과한 검증만 `PASS`로 보고한다.

## 버전·배포·결과물

- 공식 소스 변경은 버전을 증가시키며 같은 버전의 다른 Integrated ZIP을 만들지 않는다.
- 검증된 단일 Integrated ZIP을 다음 개발 기준본으로 사용한다.
- 사용자 승인 없이 Production을 변경하지 않는다. Integration 배포도 요청·승인 범위를 확인한다.

## 외부 Skill 안전정책

1. 외부 Skill의 `SKILL.md`와 `scripts/`를 먼저 읽는다.
2. Secret, 환경변수, Git, 배포, 파일 삭제, 네트워크 전송 동작을 확인한다.
3. GUI's Arc의 인증·DB·R2·배포·버전 규칙과 충돌하면 사용하지 않는다.
4. 필요한 절차만 프로젝트 전용 Skill로 변환하며 외부 Skill을 전역 기준으로 삼지 않는다.
5. 외부 Skill은 `AGENTS.md`와 이 문서보다 우선할 수 없다.
6. 검토하지 않은 Skill로 Integration 또는 Production을 자동 배포하지 않는다.

## 작업 시작과 검증

- 수정 전 사용자 요청, 읽을 파일, 수정할 파일, 화면·Worker·DB·배포 영향을 짧게 선언하고 바로 진행한다.
- 직접 관련 테스트부터 실행하고 최종 검증은 필요한 전체 범위로 한 번 수행한다.
- 범위가 예상보다 크게 확대되거나 새 권한이 필요할 때만 중단하고 설명한다.

## 사용자 액션 피드백 원칙

- 사용자가 실행하는 모든 의미 있는 비동기 액션은 실행 전, 처리 중, 성공, 실패, 필요 시 재시도 상태를 제공한다.
- 처리 중에는 실행 버튼을 즉시 비활성화하고 `aria-busy="true"`와 구체적인 한국어 문구를 표시하여 중복 실행을 막는다.
- 1초 이상 걸릴 수 있는 작업은 무엇을 처리 중인지 문장으로 알린다. 실제 진행률을 계산할 수 없으면 임의의 백분율을 표시하지 않는다.
- 성공 문구는 “완료”만 표시하지 않고 변경된 업무와 건수를 명시한다. 서버 결과를 화면 데이터에 즉시 반영한다.
- 실패 시 입력값·선택·작성 내용은 가능한 범위에서 보존하며, 내부 오류 대신 사용자가 취할 다음 행동을 한국어로 안내한다.
- Revision 충돌은 일반 실패와 구분하고 목록 새로고침 동선을 제공하며 다른 사용자의 변경을 자동으로 덮어쓰지 않는다.
- 중복 결과 위험이 있는 서버 변경은 버튼 비활성화와 기존 Idempotency 또는 중복 방지 계약을 함께 사용한다.
- 확인 Dialog는 처리 중 닫기와 재실행을 차단하고 성공 시 닫으며 실패 시 입력과 Dialog를 유지한다.
- 상태는 색상이나 아이콘만으로 표현하지 않고 `role="status"`, `role="alert"`, `aria-live`, `aria-busy`, `disabled`를 용도에 맞게 제공한다.
- 공통 피드백 유틸리티와 스타일을 우선 재사용하며 화면마다 서로 다른 임시 Toast·Spinner를 만들지 않는다.

## 액션 피드백 완료 보고

- 처리 중·성공·실패·중복 방지·상태 보존·화면 갱신·접근성 구현 여부를 완료 보고에 명시한다.
- 실제 Browser 검증을 실행하지 않았으면 `NOT_EXECUTED`로 표시하며 UX 완료를 과장하지 않는다.

## 프로젝트 Skill 운영

- `.agents/skills`의 상위 실행 Skill과 전문 Skill을 역할별로 분리하고 동일 책임을 중복 생성하지 않는다.
- 사용자 요청과 실제 코드 범위에 필요한 Skill만 선택하고, 실제로 읽은 Skill만 적용했다고 보고한다.
- Platform은 계정·회사·현장·Membership·역할·권한의 원본이며 Issue·OS·Safety·Quality는 이를 임의 복제하지 않는다.
- Skill 정적 검증, Trigger 평가 자료와 실제 Codex 자동 발견 E2E는 서로 다른 결과로 보고한다.
- 실행하지 않은 테스트·Browser E2E·Migration·배포를 PASS 또는 완료로 표시하지 않는다.
- Pages·Workers·D1·R2 환경을 혼동하지 않고 원격 Migration 목록을 확인한 뒤 순서대로 적용한다.
- Integrated ZIP 기준으로 버전·문서·SHA-256을 동기화한다.
- 프로젝트 릴리스, 개발도구·Skill, 웹 캐시 버전의 장기 분리 필요성은 기술부채로 기록하되 v0.24.4에서 체계를 새로 분리하지 않는다.
