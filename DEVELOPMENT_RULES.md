# GUI's Arc 개발 규칙

## 1. 기준과 범위

- 작업 시작 문서는 `AGENTS.md`, `VERSION.md`, `DEVELOPMENT_RULES.md` 세 개다.
- 현재 요청을 한 문장으로 확정하고 관련 파일과 영향 범위를 먼저 확인한다.
- 제품 기능, 데이터, 권한, 배포를 요청 범위 밖에서 변경하지 않는다.
- 오래된 계획과 Handoff는 기록이며 현재 작업을 결정하지 않는다.

## 2. 오류 대응

오류는 `재현 → 원인 확인 → 최소 수정 → 회귀 테스트 → 임시 규칙 폐기`로 끝낸다.

- 자동 검출 가능한 오류는 테스트·타입·스키마·코드 제약으로 남긴다.
- 특정 오류 절차는 상시 규칙이 아니라 조건부 runbook으로 둔다.
- 임시 규칙에는 적용 범위, 발동 조건, 종료 조건, 만료 시점을 적는다.
- 임시 규칙은 오류 해결 또는 다음 공식 checkpoint에서 폐기한다.

## 3. 상시 규칙 추가 기준

새 상시 규칙은 여러 모듈 공통성, 코드 강제 난이도, 반복성 또는 치명성, 예방 효과와 유지 비용을 모두 검토한다. 규칙 하나를 추가하면 기존 규칙 하나를 삭제하거나 통합한다.

## 4. 위험도 기반 검증

- 문서·주석: 관련 문서 검사, `git diff --check`.
- CSS·문구·레이아웃: focused test, build, 한국어 변경 시 Korean UI, 모바일 변경 시 관련 viewport.
- Frontend 동작: focused test, 직접 회귀 테스트, build. 터치·카메라·음성·canvas 변경에만 실제 브라우저 또는 모바일 smoke.
- Worker/API: endpoint allow·deny·error focused test와 변경 파일 syntax. 공통 인증·세션·응답 변경에만 전체 테스트.
- D1·R2·Migration: 관련 테스트, 전체 테스트, validate, 실행 전후 읽기 검증, 사용자 승인.
- Pages 배포: 관련 테스트, build, static revision·Service Worker 확인, 변경 화면 smoke.
- 공식 release checkpoint: 전체 npm test, 전체 JavaScript syntax, validate, build, 필요한 Korean UI·performance, secret scan, Commit·Push, ZIP·SHA-256 검증.

## 5. 버전과 승인

- 작은 오류 수정마다 제품 버전과 공식 ZIP을 만들지 않는다.
- static revision은 실제 정적 자산 배포에서 cache bust가 필요할 때만 변경한다.
- 여러 수정은 검증된 checkpoint에서 한 번에 release 처리한다.
- Production, D1·R2·Migration 데이터 변경, 파괴적 작업과 되돌리기 어려운 권한·보안·데이터 구조 변경은 별도 승인을 받는다.
- 처음 승인 범위에 명시된 구현·검증·Integration Pages 배포는 다시 묻지 않는다.

## 6. 코드와 사용자 액션

- 최소 변경으로 기존 데이터·API·권한 계약을 보존한다.
- 비동기 사용자 액션은 처리 중·성공·실패 상태와 중복 실행 방지를 제공한다.
- 실패 시 입력과 선택을 보존하고 접근 가능한 피드백을 제공한다.
- Mock·UI-only·Local-only 구현을 실제 완료로 보고하지 않는다.

## 7. 관리 화면 UX

핵심 원칙은 “같이 처리하는 정보는 압축하고, 성격이 다른 기능은 분리한다.”이다.

- 불필요한 세로 스크롤과 반복 설명을 줄이고, 함께 판단하는 정보는 같은 행이나 영역에 압축한다.
- 기능이나 의사결정이 달라지는 지점은 행·구분선·섹션으로 분리한다.
- PC에서는 수평 공간을 활용하고 모바일에서는 잘림이나 가로 스크롤 없이 자연스럽게 세로 배치한다.
- 복잡한 선택은 현재 값을 요약하고 변경할 때만 선택 영역을 펼칠 수 있다.
- 같은 그룹의 입력·셀렉트·버튼은 높이, 시작선, 정렬과 간격을 일관되게 맞춘다.
- 새 관리 화면은 공통 Form·Grid·Table·Toolbar를 우선 재사용하며, 공통 구조로 해결할 문제를 화면별 문서 규칙으로 늘리지 않는다.
- 정렬 붕괴, 겹침, 글자·버튼·입력 잘림, 모바일 사용 불가, 같은 그룹의 명백한 입력 크기 불일치는 개발 중 즉시 수정한다.
- 미세한 색상·그림자·둥근 모서리·1~2px 차이처럼 기능과 사용성에 영향 없는 장식은 개발을 중단하지 않고 UI polishing으로 묶는다.

## 8. Git과 산출물

- 기존 사용자 변경을 임의로 삭제하거나 되돌리지 않는다.
- Commit·Push·ZIP은 공식 release 승인 범위에서만 실행한다.
- force push, 파괴적 reset, secret·환경 파일의 Commit을 금지한다.
- 공식 ZIP은 검증된 commit snapshot으로 만들고 SHA-256을 확인한다.

## 9. 보고

- 일반 보고는 결과, 변경 파일, 테스트 결과, 영향·위험, 사용자 확인 항목 하나로 끝낸다.
- 실행하지 않은 항목과 변경하지 않은 리소스는 필요한 경우에만 표시한다.
