import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";const read=file=>fs.readFileSync(file,"utf8");
test("constitution and decisions remain complete",()=>{const constitution=read("docs/00_CONSTITUTION.md"),decisions=read("docs/07_DECISIONS.md");for(let i=1;i<=16;i++)assert.match(constitution,new RegExp(`C-${String(i).padStart(3,"0")}`));for(let i=1;i<=5;i++)assert.match(decisions,new RegExp(`GDR-${String(i).padStart(3,"0")}`))});
test("every version surface is v0.24.11",()=>{for(const file of["package.json","package-lock.json","docs/08_VERSION_MATRIX.json","build-metadata.json"])assert.equal(JSON.parse(read(file)).version,"0.24.11");for(const file of["wrangler.integration.toml","worker/version.js","apps/web/assets/version.js","README.md","apps/web/index.html"])assert.match(read(file),/0\.24\.11/);assert.match(read("apps/web/assets/app.js"),/issues\.js\?v=0\.24\.11/)});
test("module implementation truth uses READY and PLANNED",()=>{const matrix=JSON.parse(read("docs/08_VERSION_MATRIX.json"));for(const code of["today","issue","workforce","construction","safety","quality","admin"])assert.equal(matrix.modules[code],"READY");for(const code of["materials","equipment","documents"])assert.equal(matrix.modules[code],"PLANNED");for(const state of["READY","PLANNED"])assert.match(read("worker/core/permissions.js"),new RegExp(state))});
test("provisioning stays disabled and Production absent",()=>{assert.match(read("wrangler.integration.toml"),/PROVISIONING_ENABLED = "false"/);assert.ok(!/production/i.test(read("wrangler.integration.toml")))});
