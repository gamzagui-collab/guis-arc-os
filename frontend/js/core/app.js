import { state, loadLocal, saveLocal } from "./state.js";
import { renderDashboard } from "../pages/dashboard.js"; import { renderWeather } from "../pages/weather.js"; import { renderSchedule } from "../pages/schedule.js"; import { renderGuide } from "../pages/guide.js"; import { renderDatabase } from "../pages/database.js"; import { renderBriefing } from "../pages/briefing.js"; import { renderChat } from "../pages/chat.js";
const pages={dashboardPage:renderDashboard,weatherPage:renderWeather,schedulePage:renderSchedule,guidePage:renderGuide,dbPage:renderDatabase,briefingPage:renderBriefing,chatPage:renderChat};
function showMain(){document.getElementById("loginPage").classList.remove("active");document.getElementById("homePage").classList.add("active");activateSubpage("dashboardPage");}
function activateSubpage(id){document.querySelectorAll(".subpage").forEach(p=>p.classList.remove("active"));document.querySelectorAll(".tab").forEach(b=>b.classList.remove("active"));document.getElementById(id).classList.add("active");document.querySelector(`[data-page="${id}"]`)?.classList.add("active");pages[id]?.(document.getElementById(id));}
function init(){loadLocal();document.getElementById("guestStartBtn").addEventListener("click",()=>{state.mode="guest";state.site={siteName:"게스트 현장",siteType:"임시"};saveLocal();showMain();});document.getElementById("createDemoSiteBtn").addEventListener("click",()=>{state.mode="site";state.site={siteCode:"DEMO-001",pin:"1234",siteName:"김제 샘플현장",siteType:"학교"};saveLocal();alert("샘플 현장: DEMO-001 / PIN: 1234");showMain();});document.getElementById("siteLoginBtn").addEventListener("click",()=>{const siteCode=document.getElementById("siteCodeInput").value.trim();const pin=document.getElementById("sitePinInput").value.trim();if(!siteCode||!pin){alert("현장코드와 PIN을 입력하세요.");return;}state.mode="site";state.site={siteCode,pin,siteName:siteCode,siteType:"미지정"};saveLocal();showMain();});document.querySelectorAll(".tab").forEach(btn=>btn.addEventListener("click",()=>activateSubpage(btn.dataset.page)));}
init();
