import { getDemoWeatherSummary, riskClass } from "../services/weatherEngine.js";
import { WEATHER_MODELS } from "../services/weatherModels.js";
function blueRainStyle(v){if(v<=0)return "";const alpha=Math.min(.92,.10+v/12);const color=v>=7?"color:#fff;font-weight:1000;":"color:#0f172a;font-weight:900;";return `style="background:rgba(37,99,235,${alpha});${color}"`;}
function rainCell(value,extraClass=""){return `<td class="${extraClass}" ${blueRainStyle(value)}>${value.toFixed(1)}</td>`;}
function graphBars(rows){const max=Math.max(1,...rows.map(h=>h.rain1h));return rows.map(h=>{const height=Math.max(4,Math.round((h.rain1h/max)*120));return `<div class="rain-hour-col"><div class="rain-bar-slot"><div class="rain-graph-bar blue-bar" style="height:${height}px"></div></div><div class="rain-value">${h.rain1h.toFixed(1)}</div><div class="rain-hour">${h.hour}시</div></div>`;}).join("");}
function mapCards(){return `<section class="weather-map-grid"><article class="card map-card"><h3>🗺 좌표 선택 지도</h3><p class="help-text">지도를 클릭하면 해당 좌표의 위치명을 확인하고 예보를 조회할 수 있습니다.</p><div class="fake-map local-map"><div class="map-pin">●</div><div class="map-label">김제시</div></div></article><article class="card map-card"><h3>☁ Windy 강수 지도</h3><p class="help-text">비구름과 강수 흐름을 확인합니다.</p><iframe class="windy-frame" title="Windy map" src="https://embed.windy.com/embed2.html?lat=35.8036&lon=126.8809&detailLat=35.8036&detailLon=126.8809&width=650&height=450&zoom=7&level=surface&overlay=rain&product=ecmwf&menu=&message=&marker=true&calendar=now&pressure=&type=map&location=coordinates&detail=&metricWind=m%2Fs&metricTemp=%C2%B0C&radarRange=-1"></iframe></article></section>`;}
export function renderWeather(root){const w=getDemoWeatherSummary();const todayRows=w.todayRows;root.innerHTML=`<div class="section-head compact-head"><div><h2>WEATHER · 6일 1시간 강수 비교</h2><p>1시간 강수, 예보처 비교, 작업 가능 시간, 지도 정보를 한 화면에서 확인합니다.</p></div><div class="export-row"><button class="secondary-btn" onclick="window.print()">출력</button><button class="secondary-btn">CSV 저장</button></div></div><div class="kpi-row weather-kpi"><div class="kpi"><span>오늘 예상 강수량</span><strong>${w.maxRain.toFixed(1)}mm</strong><small>${w.peakRain.hour}시 최대</small></div><div class="kpi"><span>체감온도</span><strong>${w.maxApparent.toFixed(1)}℃</strong><small>최고 체감</small></div><div class="kpi"><span>평균 풍속</span><strong>${w.maxWind.toFixed(1)}m/s</strong><small>약간 강함</small></div><div class="kpi"><span>작업 추천 시간</span><strong>${w.workWindow.split(" ")[0]}</strong><small>오전 작업 권장</small></div><div class="kpi"><span>콘크리트 타설 판정</span><strong>${w.concrete.overall}</strong><small>오후 시간 피하세요</small></div><div class="kpi"><span>오늘 일치도</span><strong class="star-text">${w.todayConsistency}</strong><small>예보처 편차 기준</small></div></div>${mapCards()}<section class="weather-main-grid"><article class="card weather-chart-card"><h3>오늘 1시간 강수 그래프</h3><div class="rain-graph-fixed">${graphBars(todayRows)}</div><p class="chart-note">막대값은 예보처별 1시간 강수량 중 최대값입니다.</p></article><article class="card ai-weather-card"><h3>🤖 오늘 강수 AI 의견</h3><p>${w.aiOpinion}</p><ul><li>강수 집중 시간에는 타설·방수·외부마감 작업을 피하세요.</li><li>배수로, 보양재, 자재 방수 조치를 사전에 확인하세요.</li><li>예보처 편차가 큰 시간대는 현장 판단 전 최신 예보를 재조회하세요.</li></ul></article></section><section class="card"><h3>콘크리트 타설 판단</h3><div class="summary-grid">${w.concrete.checks.map(d=>`<article class="summary-card ${riskClass(d.level)}"><div class="label">${d.title}</div><p>${d.text}</p></article>`).join("")}</div></section><section class="card"><div class="section-head mini-head"><div><h3>6일 시간별 강수 비교표</h3><p>예보처 4곳은 동일한 하늘색 계열, 평균/최대/일치도는 별도 열로 구분했습니다.</p></div><span class="unit-label">(단위: mm)</span></div><div class="table-scroll sticky-weather-wrap"><table class="weather-compare-table weather-v93"><thead><tr><th class="sticky-col-date">날짜</th><th class="sticky-col-hour">시간</th>${WEATHER_MODELS.map(m=>`<th class="model-head">${m.name}<br><small>${m.label}</small></th>`).join("")}<th class="avg-head">평균</th><th class="max-head">최대</th><th class="consistency-head">일치도</th></tr></thead><tbody>${w.hourly.map(h=>`<tr class="${h.hour==="00"?"date-start-row":""}">${h.hour==="00"?`<th class="date-cell sticky-col-date" rowspan="24"><span>${h.dateLabel}</span><small>${h.weekdayLabel}</small></th>`:""}<th class="sticky-col-hour">${h.hour}시</th>${WEATHER_MODELS.map(m=>rainCell(h.modelRain[m.id]||0,"model-cell")).join("")}${rainCell(h.avgRain1h,"avg-cell")}${rainCell(h.rain1h,"max-cell")}<td class="star-cell">${h.stars}</td></tr>`).join("")}</tbody></table></div><p class="chart-note">0.0mm는 무색, 강수량이 커질수록 옅은 파랑에서 진한 파랑으로 표시됩니다.</p></section>`;}
